

export const ColorPrimary = (opacity: number) => {
    return `rgba(135, 233, 226, ${opacity})`;
}
export const ColorSecondary = (opacity: number) => {
    return `rgba(18, 152, 142, ${opacity})`;
}

export const ColorSecondaryBlackOverlay = (opacity: number) => {
    return `rgba(8, 46, 43, ${opacity})`;
}

